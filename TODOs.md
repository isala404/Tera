# Audit TODOs

Quick local audit on 2026-09-12, against commit `3bdbd24`. Reviewed conversation routing, persistence, MCP, scheduling, recovery, logging, workspace setup and release configuration. P1 means a delivery, data or credential risk; P2 means a verification or modeling gap; P3 means cleanup. Items are based on code inspection unless marked **Reproduced**. This is not an exhaustive security or dependency audit.

Verification: `cargo test --all-targets --offline` passed with 196 tests and four ignored live Codex tests. The three Python suites passed all 26 tests. `cargo clippy --all-targets --offline -- -D warnings` passed. `cargo fmt --all -- --check` failed at `src/codex/process.rs:752`. Five temporary probes confirmed the defects marked below; those probes asserted the current broken behavior and were removed after execution. Live WhatsApp, live model calls and Linux service behavior were not exercised.

## Verification gates that do not exist yet

- [x] **P2 — Run verification before merging and releasing.** The only [workflow](/Users/isala/Projects/isala/Tera/.github/workflows/release.yml:1) runs on version tags and builds binaries without tests, Clippy or formatting checks. Add a pull request workflow and make release publication depend on the same checks. Fix the existing formatting failure. Verify: Rust tests, the three Python suites, Clippy and rustfmt all exit zero; a failing check prevents publication.

- [x] **P2 — Exercise process and recovery failures without a real account.** [Live integration tests](/Users/isala/Projects/isala/Tera/tests/codex_live_test.rs:38) are ignored, and existing tests do not cover the event overflow, overlapping workers and interrupted writes listed below. Add a deterministic fake app-server and fault injection at those boundaries. Include a Linux service smoke check for the shipped unit. Verify: failures terminate or recover within bounded test deadlines without network access or account tokens; keep real-provider checks explicitly opt-in.

## Dead code and dead surface

- [x] **P3 — Remove the disconnected model-observation feature.** [ModelDiscovery](/Users/isala/Projects/isala/Tera/src/codex/models.rs:15) has no production caller; its tests are the only callers of `process_models_response`. The associated [runtime observation methods](/Users/isala/Projects/isala/Tera/src/runtime/state.rs:248) consequently serve only this dead path. Remove the unused implementation and its public surface, leaving existing database rows intact. Verify: a repository reference search finds no remaining consumers or claims that this path detects model changes, and tests and Clippy pass. Preserve the process manager's model-list lookup, which is used to resolve native defaults.

## One fact written many times

- [x] **P1 — Derive every reply target from the latest event. Reproduced.** [Burst creation](/Users/isala/Projects/isala/Tera/src/conversation/engine.rs:393) captures the first provider message ID, while [reply construction](/Users/isala/Projects/isala/Tera/src/conversation/engine.rs:606) uses the last event's text. Joining a burst or successfully steering does not refresh the durable reply target. Two messages left `last_provider_msg_id` pointing at the first. Store one authoritative latest event reference and resolve its provider metadata together. Verify: a two-message burst and a steering message quote the newest message with matching ID, text and sender side, including after recovery.

- [x] **P3 — Bring operational documentation back into agreement with code.** The [README](/Users/isala/Projects/isala/Tera/README.md:110) says SIGTERM does not perform cleanup, but [main](/Users/isala/Projects/isala/Tera/src/main.rs:717) handles it. The [service comments](/Users/isala/Projects/isala/Tera/deploy/tera.service:1) reference a missing `PLAN.md`. Comments in [conversation execution](/Users/isala/Projects/isala/Tera/src/conversation/engine.rs:554) and [isolated worker APIs](/Users/isala/Projects/isala/Tera/src/codex/supervisor.rs:274) still describe maintenance interruption that has no caller. Update these claims to current behavior. Verify: signal smoke tests match the deployment instructions, and searches find no obsolete plan references or claims of automatic maintenance preemption.

## Data shapes that make the code harder than it is

- [x] **P2 — Make schedule timing and lifecycle states explicit types.** [ScheduleTiming](/Users/isala/Projects/isala/Tera/src/scheduler/recurrence.rs:31) and [ScheduleItem](/Users/isala/Projects/isala/Tera/src/scheduler/db.rs:47) combine a string discriminator with optional fields that permit invalid combinations. `dtstart_local` is always created as `None`. Run and conversation states also accept arbitrary strings. Use `Once` and `Recurring` variants and closed lifecycle enums at the Rust boundary; preserve stored spellings and compatibility with existing databases. Verify: invalid timing combinations cannot be constructed through the application API, and unknown persisted states return explicit errors.

- [x] **P2 — Give message and reaction events distinct payloads.** [ConversationEvent](/Users/isala/Projects/isala/Tera/src/history/db.rs:101) allows a reaction without a target or emoji, and [projection](/Users/isala/Projects/isala/Tera/src/history/projection.rs:65) replaces those missing values with empty strings. Model required reaction fields in a variant instead of relying on unrelated options. Verify: malformed reaction rows fail explicitly, and valid message/reaction JSONL output stays compatible.

## Correctness

- [x] **P1 — Commit an event and its dependent rows atomically. Reproduced.** [Event insertion](/Users/isala/Projects/isala/Tera/src/history/db.rs:204) autocommits the event before inserting attachments; provider references and delivery rows are separate writes too. An injected attachment-insert failure returned an error while leaving a canonical event without its attachment or projection. Wrap the canonical unit of work in a SQLite transaction and project only after commit. Verify: injected failure at each dependent insert leaves no partial event, attachment or provider mapping.

- [x] **P1 — Deduplicate inbound provider messages before accepting them. Reproduced.** [Inbound handling](/Users/isala/Projects/isala/Tera/src/conversation/engine.rs:184) always creates a fresh event; [provider mapping](/Users/isala/Projects/isala/Tera/src/history/db.rs:276) uses `INSERT OR REPLACE`. Delivering the same provider ID twice created two events and removed the first event's reverse mapping. Make acceptance atomic and idempotent by provider identity, before secret capture or turn routing can repeat side effects. Verify: sequential and concurrent replays produce one event, one mapping and one accepted input, including across restart.

- [x] **P1 — Scope delivery accounting and history attribution to each worker.** [ConversationSession](/Users/isala/Projects/isala/Tera/src/conversation/session.rs:16) shares one turn ID and send counter across foreground work, scheduled work and Phoenix. [MCP sends](/Users/isala/Projects/isala/Tera/src/mcp/daemon_rpc.rs:279) take their turn ID from that shared slot. A scheduled send during a foreground turn increments the counter that [suppresses the foreground fallback](/Users/isala/Projects/isala/Tera/src/conversation/engine.rs:698); Phoenix also overwrites and clears the slot. Carry worker identity through the MCP boundary. Verify: overlapping foreground, schedule and recovery turns retain their own history attribution and cannot suppress each other's replies.

- [x] **P1 — Never discard terminal Codex events when a queue fills.** [Dispatch and failure delivery](/Users/isala/Projects/isala/Tera/src/codex/process.rs:786) ignore `try_send` failures into a [256-event queue](/Users/isala/Projects/isala/Tera/src/codex/process.rs:877). Dropping `Completed` or `Failed` leaves `collect_turn` waiting while its sender remains in the listener map. Deliver terminal state reliably and clean up listeners when callers are cancelled. Verify: more than 256 queued events followed by completion, process death or cancellation always releases the waiting turn within a test deadline.

- [x] **P1 — Check projection contents, not just line counts. Reproduced.** [Projection verification](/Users/isala/Projects/isala/Tera/src/history/projection.rs:169) and [history health](/Users/isala/Projects/isala/Tera/src/history/backup.rs:81) accept matching counts. Replacing the sole JSONL record with `not JSON` still reported healthy and skipped repair. Validate parsing and correspondence with canonical records. Verify: invalid JSON, duplicate IDs, changed text and missing records fail the health check and are repaired; a valid projection is unchanged.

- [x] **P1 — Preserve outbound attachments in canonical history.** [MCP send_message](/Users/isala/Projects/isala/Tera/src/mcp/daemon_rpc.rs:239) sends media but records `attachments: vec![]`. A file-only reply therefore has neither text nor an asset reference in history, and an output in scratch storage can later disappear. Save a durable asset and its metadata through the same canonical recording path. Verify: each supported outgoing media kind can be recovered from history after deleting the original scratch file and rebuilding JSONL.

## Failures that stay silent

- [x] **P1 — Fail closed when pending credential capture cannot be read. Reproduced.** [capture_secret](/Users/isala/Projects/isala/Tera/src/conversation/engine.rs:285) passes ordinary text through after a secret-store read error. Arming a request, corrupting the store and sending a synthetic reply put the reply verbatim into history. Preserve enough pending-request state to reject uncertain credential input, and report the capture failure without echoing the payload. Verify: corrupt, unreadable or unexpectedly missing storage cannot put a pending credential reply into SQLite, JSONL, logs or model input.

- [x] **P1 — Treat failure to close a scheduled run as a real failure.** [Scheduler completion](/Users/isala/Projects/isala/Tera/src/scheduler/runner.rs:230) ignores `finish_run` errors, removes recovery context and logs completion. The row can remain `running`, causing Phoenix to execute already-completed work again after restart. Propagate or explicitly recover the persistence error before declaring completion. Verify: an injected terminal-state write failure is observable and cannot silently turn a completed side effect into an ordinary retry.

- [x] **P2 — Distinguish absent media from a failed download.** [fetch_media](/Users/isala/Projects/isala/Tera/src/transport/whatsapp.rs:227) returns `None` for both. A failed file-only message then fails `is_answerable` and receives no reply; a captioned message reaches the model without any indication that its attachment failed. Represent download failure explicitly and report it to the owner. Verify: failed file-only and captioned downloads both produce a clear failure outcome while ordinary text remains unaffected.

- [x] **P2 — Stop showing database errors as empty status results.** [Status](/Users/isala/Projects/isala/Tera/src/main.rs:395) uses `unwrap_or_default` for schedules and recent runs. A query failure can look like zero active schedules. Print the actual degraded component and return a useful failure status. Verify: a malformed schedule table yields an explicit diagnostic rather than a successful empty listing.

## Copy-paste that has not diverged yet

- [x] **P3 — Keep each SQL projection's column order in one place.** Four [history queries](/Users/isala/Projects/isala/Tera/src/history/db.rs:328) repeat the same ordered columns consumed by `row_to_event`; three [schedule queries](/Users/isala/Projects/isala/Tera/src/scheduler/db.rs:174) do the same for `schedule_from_row`. Share these exact column projections locally so adding a field cannot update only one query. Verify: direct lookup, full listing, recent history, turn history and due-schedule reads all reconstruct the same fixture fields. Avoid introducing a generic query framework.

## Logs and traceability

- [x] **P1 — Apply credential redaction before logging or fallback persistence.** [Daemon RPC logging](/Users/isala/Projects/isala/Tera/src/mcp/daemon_rpc.rs:132) logs raw tool arguments before `send_message` redacts them. [Codex logging](/Users/isala/Projects/isala/Tera/src/codex/log.rs:143) also records command output and tool arguments, while [foreground fallback](/Users/isala/Projects/isala/Tera/src/conversation/engine.rs:709) and [Phoenix fallback](/Users/isala/Projects/isala/Tera/src/conversation/phoenix.rs:200) bypass the MCP redaction path. A stored credential pasted by the model can therefore survive in logs or fallback history despite the existing MCP tests passing. Verify with synthetic credentials: all these paths keep values out of logs, SQLite and JSONL while intended placeholder expansion still works on the wire.

- [x] **P2 — Persist the Codex thread that belongs to each scheduled run.** [schedule_runs.codex_thread_id](/Users/isala/Projects/isala/Tera/src/scheduler/db.rs:36) is selected but never populated; [run_task_turn](/Users/isala/Projects/isala/Tera/src/codex/supervisor.rs:299) hides the generated thread ID from the scheduler. Record it at thread creation and include run/thread IDs in completion and failure logs. Verify: every started worker has a durable run-to-thread mapping that remains available after failure or restart.

## Boundaries the architecture draws

- [x] **P2 — Apply owner filtering before downloading attachments.** [The WhatsApp callback](/Users/isala/Projects/isala/Tera/src/transport/whatsapp.rs:343) downloads media before [the engine's owner gate](/Users/isala/Projects/isala/Tera/src/conversation/engine.rs:151). Rejected senders and groups therefore still trigger download and allocation work. Evaluate the existing owner policy on message metadata before fetching media. Verify: rejected messages cause zero downloads, history writes or turns, and accepted owner attachments still arrive normally.

- [x] **P1 — Make the MCP listener part of daemon readiness and supervision.** [Startup](/Users/isala/Projects/isala/Tera/src/main.rs:687) spawns the RPC server, logs its failure inside the detached task, then calls `mark_healthy` without waiting for its socket to bind. The daemon can announce readiness and commit an update while its tool server has failed. Establish listener readiness before reporting health and surface later task failure to the daemon supervisor. Verify: an injected bind failure prevents the ready state and preserves update recovery information; loss of a required task produces an observable daemon failure.

## Repo hygiene: what git carries

No tracked cleanup was demonstrated. The initial working tree was clean. Inspection of all 93 tracked paths found no build directories, Python caches, SQLite runtime files, `.env`, `auth.json`, `secrets.json` or files larger than 1 MiB. `Cargo.lock` is tracked, and release builds use `--locked`. This was a path and size check, not a scan of historical commits for credentials.

## Verified clean (no action)

- The existing 196 Rust tests, 26 Python tests and Clippy passed. Formatting is the outstanding baseline check listed above.
- Owner-policy tests cover the paired-account default, configured owner matching, device suffixes and group rejection. Keep this gate; the placement before media download still needs the boundary fix above.
- Workspace tests verify idempotent initialization, preservation of user model configuration and personal files, generated-template refresh and preservation of owner-created skills.
- Existing tests verify SQLite-backed backups, missing-asset reporting, projection repair when record counts drift, provider reply translation and normal credential capture. These checks remain valuable; the reproduced failure cases above are outside their current coverage.
- Full-access Codex execution and host-local schedule time are explicit product choices documented by this repository. No change to either policy is proposed by this audit.
