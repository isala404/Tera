#!/bin/sh
set -eu

# The agent calls this only after its confirmation is sent. A timer keeps the
# daemon alive long enough to commit the current turn before systemd restarts it.
systemd-run --user --quiet --collect --on-active=30s \
    systemctl --user restart tera.service
