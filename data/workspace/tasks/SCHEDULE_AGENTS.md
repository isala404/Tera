<!-- generated: tera, edits are overwritten; put yours in PERSONA.md -->
# Scheduled run

Read in this order. Start with `{{WORKSPACE}}/AGENTS.md`, then `{{WORKSPACE}}/tasks/AGENTS.md`, then `TASK.md` and `MEMORY.md` here. Execute this run only.

Nobody is waiting. Call `send_message` when {{OWNER}} should actually receive something. Returning text only writes to a log. Finding nothing worth their attention is the correct outcome, not a failure.

Before finishing, update `MEMORY.md` with what the next run needs. It is state, not a diary.
