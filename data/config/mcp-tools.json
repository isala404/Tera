[
  {
    "name": "send_message",
    "description": "Send a WhatsApp message to {{OWNER}}. This is the ONLY way to reach them. Text you return at the end of a turn is a fallback inside a conversation, and nothing at all from a scheduled task. Plain text, no markdown: no headings, bold, bullet lists or tables. Keep messages short and vary their length naturally. If an answer would be dense, send several short messages split at thought boundaries. Do not pad a short answer. Every line in the transcript is prefixed with its message id, so pass one as reply_to when you are answering something specific and a bare message would be ambiguous; skip it in ordinary back-and-forth, where a quoted reply is just noise. To send a credential you cannot see, write ${NAME} where the value belongs, e.g. client_id=${SPOTIFY_CLIENT_ID}, and Tera substitutes it on the way out, so {{OWNER}} gets a working link while history keeps only the placeholder.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "text": { "type": "string", "description": "Message text. Plain, short, no markdown." },
        "image_path": { "type": "string", "description": "Path to an image file: absolute, or relative to the workspace root" },
        "video_path": { "type": "string", "description": "Path to a video file: absolute, or relative to the workspace root" },
        "audio_path": { "type": "string", "description": "Path to an audio file: absolute, or relative to the workspace root" },
        "file_path": { "type": "string", "description": "Path to any other file to send as a WhatsApp document: absolute, or relative to the workspace root" },
        "reply_to": { "type": "string", "description": "Message id to quote, as shown in the transcript, e.g. msg_a1b2. Use it when answering one specific message out of several." }
      }
    }
  },
  {
    "name": "react",
    "description": "React to a message with an emoji. Often the right answer instead of a message: acknowledging something, agreeing, or when the emoji is the whole reply. Costs them nothing to read.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "message_id": { "type": "string", "description": "Internal event id of the message to react to, e.g. msg_a1b2. Not a WhatsApp id." },
        "emoji": { "type": "string", "description": "Emoji character (e.g. ❤️, 👍)" }
      },
      "required": ["message_id", "emoji"]
    }
  },
  {
    "name": "schedule",
    "description": "Schedule a one-shot or recurring background task. Use this for anything time-based; never install a cron job or launchd plist. All times are this machine's local time. The run happens on a fresh thread in its own task directory with no memory of this conversation, so write `prompt` as standalone instructions: what to do, where to look, and what is worth messaging {{OWNER}} about. The response echoes back the resolved first run. Check it says what you meant.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "Short descriptive name, e.g. 'Morning brief'" },
        "prompt": { "type": "string", "description": "Standalone instructions for the worker. It has no conversation context. Say what to do, where the inputs are, and when to message {{OWNER}} versus finish silently." },
        "timing": {
          "type": "object",
          "description": "When to run. One-shot: {\"type\":\"once\",\"at\":\"2026-08-18T07:30:00+05:30\"}, RFC3339 with an offset, and it must be in the future. Recurring: {\"type\":\"recurring\",\"rrule\":\"...\"} where rrule is either a cron expression evaluated in local time or a fixed interval EVERY_15M / EVERY_2H / EVERY_3D. Cron is ordinary 5-field crontab: \"30 7 * * *\" is 07:30 daily, \"0 9 * * MON\" is 09:00 Mondays, \"0 18 * * MON-FRI\" is weekday evenings. Day-of-week numbers follow crontab (0 and 7 are Sunday), but prefer the three-letter names, they cannot be misread. An unparseable rrule is rejected, not silently turned into something else.",
          "properties": {
            "type": { "type": "string", "enum": ["once", "recurring"] },
            "at": { "type": "string", "description": "One-shot only. RFC3339 with a UTC offset. Must be in the future." },
            "rrule": { "type": "string", "description": "Recurring only. Cron expression in local time, or EVERY_<n>M / EVERY_<n>H / EVERY_<n>D." }
          },
          "required": ["type"]
        },
        "tier": {
          "type": "string",
          "enum": ["routine", "default", "heavy"],
          "description": "How much model to spend on each run. 'routine' (gpt-5.6-luna, low effort) for mechanical work of known shape. Checks, sweeps, assembling a brief from known sources. This is the right answer for most schedules. 'default' (gpt-5.6-luna, xhigh) when the run needs real judgement. 'heavy' (gpt-5.6-sol, high) only when the thinking is genuinely hard, or {{OWNER}} asked for sol. Defaults to 'routine'."
        }
      },
      "required": ["name", "prompt", "timing"]
    }
  },
  {
    "name": "list_schedules",
    "description": "List active schedules with their next run in local time and their tier. Use it before creating something that may already exist, and to check that a schedule you just made fires when you intended.",
    "inputSchema": { "type": "object" }
  },
  {
    "name": "cancel_schedule",
    "description": "Cancel an active schedule. A recurring task that has stopped being useful is clutter someone will eventually have to ask about, so cancel it rather than leaving it running.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "schedule_id": { "type": "string", "description": "Schedule ID to cancel, e.g. sched_a1b2" }
      },
      "required": ["schedule_id"]
    }
  },
  {
    "name": "request_secret",
    "description": "Ask {{OWNER}} for a credential a skill needs: an API key, client ID or token. Sends them a short message and claims their next message as the value, which Tera stores where skills read it. You never see the value, not now and not later. It is taken out of the message before it reaches you, so there is no point asking for it in plain conversation and no point reading it back to check. Always use this rather than asking in your own words: a credential typed into an ordinary message ends up in history, in the JSONL projection, and in every memory generation built from it. Check `.runtime/secrets.json` first, it may already be stored. The request expires in 15 minutes.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "name": { "type": "string", "description": "Env-var shaped name: A-Z, digits and underscore, starting with an uppercase letter, e.g. SPOTIFY_CLIENT_ID" },
        "reason": { "type": "string", "description": "What it is for, as a short phrase completing 'I need NAME to ...'. Shown to {{OWNER}} verbatim, e.g. 'control your Spotify'." }
      },
      "required": ["name", "reason"]
    }
  }
]
