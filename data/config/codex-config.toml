# User-owned native Codex configuration. Tera never rewrites this file.
# Leave it empty to use Codex's defaults. When the owner asks for another model,
# add the normal Codex model and provider settings here, then restart Tera.
